# AGENTS.md

**开工前先完整读一遍 [`CLAUDE.md`](CLAUDE.md)**，这个仓库的名称约定、干活纪律和文档分工都在那里。

这里不复述内容——同一个事实写在两个文件里，早晚会分岔。**Codex 不会自动展开 `@路径`**（已实测，见 `skills/contour/references/load.md`），所以这条是普通指针，需要你主动去读。

三条最容易踩的，先记住：

1. **平台能力不许凭印象说**——要么引 `docs/平台能力实测.md`，要么去查。搜不到 ≠ 做不到。
2. **结论要标证据等级**，七类不互相顶替；空着不写等于说"验过且正常"，一律写 `[未验证]`。
3. **`skills/contour/` 还是旧架构**，跟 `docs/新知界需求.md` 对不上，只能当待审计对象。
