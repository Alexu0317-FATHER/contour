# <endpoint-id>

<!-- 放在 state/endpoints/<endpoint-id>.md。
     只由这一端自己写，所以天然无写冲突。
     endpoint-id 描述产品表面（chatgpt-web / codex-cli / claude-code……），不含模型名——
     换模型不是换端，模型名放每次倾倒的元数据里。 -->

## 基本

- **产品与表面**：
- **当前模型**：
- **账号或项目范围**：
- **原生记忆是否开启**：是 / 否 / 无法确认

## 锚点能力

<!-- 逐项实测，不接受模型口头声称「我可以访问 GitHub」。见技能的 protocol.md。 -->

| 能力 | 结果 | 验证日期 | 证据（提交/PR 标识） |
|---|---|---|---|
| 读取 |  |  |  |
| 搜索 |  |  |  |
| 创建提交 |  |  |  |
| 更新文件 |  |  |  |
| 创建 PR |  |  |  |
| 合并 PR |  |  |  |

- **接入状态**：完整 / 只读 / 未完成
- **能否当协调者**：<需要：读得到全部 inbox + 写得进统一文件 + 条件写入可用>

<!-- 写不进锚点 = 只读消费者，没有完成双向同步。别把「用户每次手工复制粘贴」
     写成日常通道。 -->

## 装与喂

- **装法**：@路径导入 / 受管区块 / 项目文件 / 只读投影
- **上次装**：　　**验证结果**：
- **上次喂**：　　**验证结果**（须开新会话验，当场复述不算）：

## 游标

- **最后一次倾倒**：<dump_id · 日期>
- **最后读到的统一版本**：
- **未提交的包**：<有就写明，下次同步重试；不许伪造成功回执>

## 探测结论

<!-- 没跑过的写「未测」，别留空——空白会被下一轮当成「测过且正常」。
     分层触发：只测这一端计划启用的能力。见技能的 probe.md。 -->

| 项 | 结果 | 日期 | 原始证据位置 |
|---|---|---|---|
| MV-04 回声隔离 | 通过 / 高风险 / 未测 |  |  |
| MV-05 项目文件隔离 | 通过 / 失败 / 未测 |  |  |
| P-1 负面项形态 | 原样 / 被改写 / 丢失 / 未测 |  |  |
| P-2 注入机制 | 确定性 / 检索 / 读不到 / 未测 |  |  |
| P-3 记忆关注策略 | 两层有效 / 仅模型层 / 无效 / 测试无效 / 未测 |  |  |

- **能力分级**：无持久召回 / 可被动召回 / 可自述倾倒 / 可核对导出
- **作用域结论**：
- **污染与回声风险**：
- **下一步**：
