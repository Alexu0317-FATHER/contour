# 知界（Contour）需求

> **本文是唯一活的产品需求文档。** 本次架构基线自 2026-08-14 起生效，背景、证据和影响范围见 [GitHub Issue #17](https://github.com/Alexu0317-FATHER/contour/issues/17)。
>
> 本文描述“接下来要做成什么”，不把尚未实现或尚未验证的能力写成现状。`skills/contour/` 与其他活文档仍含上一版 GitHub 多写者架构；在后续 Issue 完成前，它们是待迁移实现，不是本文的反证。

## 一句话定义

知界是一个以 **Codex 为唯一协调者**、以**用户本地 Git 仓库为统一记忆锚点**的跨端记忆协调技能。

它从用户明确纳入的 ChatGPT、Claude.ai 和本地 Agent 中取得可见信息，保留来源并完成去重、冲突处理和重组，再把适合各端使用的结果送回去。知界提供的是用户可审查、可迁移的统一记忆资产及其投影，不承诺直接控制各平台不可见的原生记忆数据库。

## 为什么改变架构

上一版架构要求所有端都能读写同一个 GitHub 私仓，并允许任意端临时接任协调。实际推进后，这个前提没有成立：

- ChatGPT 能看到 GitHub，不等于能按知界协议稳定创建倾倒包、回读并完成安全发布；
- Claude.ai Chat、Projects 与 Code 标签是不同环境，Code 标签不是 Claude.ai Chat 的可写 GitHub 通道，也不能代表两边记忆互通；
- 为了支持多个写入者，协议被迫引入分支引用竞态、条件写入、原子多文件发布、共享 clone 并发和多套失败恢复；
- 大量本可从官方资料或现有工具确认的能力，被错误地推迟成用户安装后的测试；
- 用户仍看不到安装后的真实交互，只看到内部状态机和 GH-XX 测试编号。

与此同时，Codex 已经具备成为本地控制面的关键条件：它能在本地项目中工作，能访问本地文件和 Git，能在用户授权范围内读取其他 ChatGPT 会话，并能通过受监督的浏览器操作协助网页端下载和上传文件。把协调权收回 Codex，能移除上一版最脆弱的“所有端都必须成为仓库写入者”假设。

因此，本次改变不是更换传输工具，而是改变产品边界：

- 从“每个端都运行完整同步状态机”改为“每个端只负责输入或接收，Codex 负责完整状态机”；
- 从“GitHub 私仓是所有人的共同工作台”改为“本地 Git 是唯一事实源，远端仓库只是可选备份或跨设备通道”；
- 从“写不进 GitHub 就未接入”改为“只要能可靠地产出或被读取、并能接收投影，就可以作为受支持端”；
- 从“尽量刷新各端原生记忆”改为“先保证统一资产和当前上下文投影可靠，再单独验证原生记忆是否吸收”。

## 当前产品范围

### 适用对象

当前 MVP 面向同时使用多个 AI 端、并拥有 Codex 本地工作环境的个人用户。用户至少需要：

- 一个可运行知界协调流程的 Codex 本地项目；
- 至少两个准备纳入统一理解的端或信息来源；
- 对被读取的会话、文件和账号拥有合法访问权；
- 愿意在网页端登录、授权、确认上传或发送等必须由本人完成的动作。

Codex 是当前架构的产品依赖，不是可随意替换的候选协调端。没有 Codex 的纯网页用户不属于当前 MVP。

### 当前要打通的端

| 端 | 在知界中的角色 | 当前证据边界 | 纳入 MVP 的条件 |
|---|---|---|---|
| Codex | 唯一协调者、统一资产写入者、当前端消费者 | 已能操作本地项目、Git 和会话；具体知界实现待改造 | 完成新状态机、唯一写入和回执验收 |
| ChatGPT | 上游会话来源，也是结果接收端 | 已实测 Codex 可按用户指定读取 ChatGPT 会话；这不等于后台记忆已共享 | 指定范围读取、增量边界和送回路径通过验收 |
| Claude.ai | 通过 Skill 生成倾倒包的上游来源，也是文件投影接收端 | 官方支持 Skills 和文件生成；知界包的下载、导入、回传闭环尚待实测 | 端到端包交换通过验收 |
| Claude Code 等本地 Agent | 通过本地交换目录生成倾倒包并读取投影 | 本地文件访问成立；知界适配器与并发边界待实现 | 不修改统一资产，只写自己的交换区 |

表中的“目标角色”不是“已经支持”。任何组合只有通过本组合的完整验收后，README 才能写成可用。

### 暂不进入当前 MVP

- 无 Codex 的纯网页组合；
- 把 GitHub、Google Drive、Notion 当作所有端共同写入的统一锚点；
- 多台设备同时协调同一个知界实例；
- 无人监督的浏览器登录、上传、发送或后台定时同步；
- 对所有 IDE、Agent 和模型做未经验证的泛化支持；
- 承诺各平台隐藏的原生记忆逐字一致或已经写入；
- 用知界替代项目管理、日历、知识库或完整的外部记忆服务。

## 产品对象与职责

### Codex：唯一协调者

只有 Codex 可以执行以下动作：

- 创建和维护知界本地实例；
- 导入各端倾倒包并校验来源、完整性和重复提交；
- 读取当前统一资产、处理冲突并生成新版本；
- 修改 `profile.md`、`now.md`、`routing.md`、`sources.md`、`evidence.md`、消费清单和端状态；
- 为不同端生成投影；
- 提交统一资产的 Git 版本；
- 在用户授权后，通过应用能力或浏览器把投影送回线上端；
- 对本轮每个动作给出可验证回执。

“唯一协调者”同时意味着**唯一规范写入者**。其他端不得直接修改统一资产、消费清单或 Git 历史，也不得临时接任合并。

同一台机器上可能同时存在多个 Codex 任务。实现必须对完整协调事务加互斥：开始前检查实例、锁和工作区状态；事务未结束时，另一个协调任务只能等待或停止，不能在同一工作树中并行编辑。

### 本地知界实例：唯一事实源

每位用户拥有一个本地知界实例。它包含：

- **统一资产**：画像、近期状态、沟通路由、信息源路由和证据；
- **不可变输入**：已导入的各端倾倒包；
- **处理状态**：哪些包已消费、生成了哪个版本；
- **端投影**：为 ChatGPT、Claude.ai 和本地 Agent 准备的材料；
- **审计历史**：由本地 Git 保存的版本、差异和撤回记录。

默认运行不依赖远端仓库。用户可以把本地 Git 连接到私有远端用于备份或跨设备迁移，但这不是当前端接入的前提，也不允许线上端直接写统一资产。

技能代码仓与个人知界实例必须分开。公开的 `Alexu0317-FATHER/contour` 只能存代码和公开文档，不能存用户画像、家庭、健康或私人倾倒包。

### 端适配器：输入与投影通道

端适配器不拥有协调权，只完成两类工作：

- **输入**：读取用户明确指定的会话或本端可见记忆，生成带来源的倾倒包；
- **输出**：接收 Codex 生成的端投影，让它影响当前上下文，并在平台允许时作为原生记忆的参考材料。

不同端可以使用不同通道，不再强求都接入同一连接器：

| 端 | 输入到 Codex | Codex 送回 |
|---|---|---|
| ChatGPT | Codex 按用户指定读取会话；不能稳定读取时，由 ChatGPT 生成文件包 | 优先使用应用内发送能力；否则由受监督浏览器上传文件或发送明确指令 |
| Claude.ai | Claude.ai Skill 生成 Markdown 倾倒包，Codex 通过浏览器下载并导入 | 受监督浏览器上传端投影，并要求在当前会话或 Project 中使用 |
| 本地 Agent | Skill 把包写入该端专属交换目录 | 直接读取端专属投影文件 |

浏览器操作是**受监督适配器**，不是稳定后台 API。每次使用必须确认账号、目标会话或 Project、准备上传的文件和最终发送状态；页面变化或工具失败必须显式停下，不能用“应该已经上传”作为成功证据。

## 数据边界

### 统一资产与原生记忆不是一回事

知界统一资产是用户可查看、可修改、可版本化的权威来源。平台原生记忆是统一资产的下游消费者之一，不是权威来源的替代品。

知界可以确认：

- 哪个源包被导入；
- 哪些结论进入了哪个 Git 版本；
- 哪个端投影已经生成；
- 哪个文件已经上传或哪个消息已经发送；
- 某个端的当前上下文是否实际读取了材料。

知界不能仅凭模型口头声称确认：

- ChatGPT 或 Claude.ai 的隐藏记忆数据库已经更新；
- 未来任意新会话一定会召回某条信息；
- 各平台保存了相同措辞或相同数量的记忆。

因此产品目标是“统一资产可靠、投影可追踪、端内使用逐步收敛”，不是“后台记忆完全一致”。

### 记忆、近期状态和权威来源分开

- `profile.md` 保存一年后大概率仍成立的慢变画像；
- `now.md` 保存会变化的用户状态和反向指针；
- `routing.md` 回答“当前怎样沟通、对这个用户什么值得留意”；
- `sources.md` 回答“最新事实应该去哪里查”和“已有知识资产在哪里”；
- 项目进度、任务、日历和业务事实仍留在各自权威系统中。

当问题包含“当前”“最近”“最新进度”“下一步”等快变含义时，Agent 不得只依赖统一记忆或平台原生记忆；能访问权威来源时必须查证，不能把旧摘要冒充现状。

## 本地目录与写入边界

```text
<contour-home>/
├── vault/                              # 本地 Git；只有 Codex 写
│   ├── config.md
│   ├── profile.md
│   ├── now.md
│   ├── routing.md
│   ├── sources.md
│   ├── evidence.md
│   ├── tests.md
│   ├── state/endpoints/<endpoint-id>.md
│   ├── dumps/inbox/<endpoint-id>/<dump-id>.md
│   ├── dumps/processed/manifest.md
│   ├── projections/<endpoint-id>/
│   └── review/
└── exchange/                           # 不作为统一事实源
    ├── from/<endpoint-id>/             # 各端只能写自己的目录
    └── to/<endpoint-id>/               # Codex 生成，其他端只读
```

`exchange/` 可以位于 `vault/` 之外，或被 Git 明确忽略。其他端生成包时不得直接写 `vault/dumps/`；Codex 校验成功后，才把包复制进 `vault/dumps/inbox/` 并在同一次事务中登记导入状态。

倾倒包一经导入不可修改。更正通过新包表达，删除通过撤回或脱敏流程表达。包只有出现在已提交版本的消费清单中，才算已经影响统一资产。

## 初始化体验

用户第一次说“初始化知界”后，Codex 必须按用户能理解的顺序完成以下流程。

### 识别实际组合

先问并自动发现用户准备纳入哪些端，再决定需要什么通道。不得先要求创建 GitHub 账号或私仓。

Codex 应区分：

- ChatGPT + Codex；
- Claude.ai + Codex；
- 一个或多个本地 Agent + Codex；
- 线上端与本地端的混合组合；
- 是否真的需要远端备份或跨设备迁移。

已有本地目录、Git 仓库或旧知界私仓时，Agent 先只读检查并提出迁移方案，不重复创建。没有本地实例时，由 Agent 创建目录和本地 Git；只有选择远端备份时，才处理 GitHub 登录、建仓和 push。

### 明确授权和范围

在读取任何跨会话内容前，Codex 必须告诉用户：

- 准备读取哪个端、哪个账号、哪个会话或 Project；
- 读取是一次性的、指定范围的，还是后续会重复使用；
- 取得的内容会保存到哪个本地目录；
- 哪些内容可能被送回其他端；
- 哪些动作需要上传、发送或远端写入。

默认只读取用户点名的会话和文件，不以“全面了解”为理由扫描全部历史。登录、验证码、账号授权和敏感范围扩大必须由用户本人确认。

### 收集基线

每个已纳入端都应生成或被读取一次基线：

- ChatGPT 基线可以来自用户指定的一个或多个会话及其项目上下文；
- Claude.ai 基线由已安装的知界 Skill 生成结构化 Markdown 包；
- 本地 Agent 基线由本端 Skill 写入自己的交换目录；
- 已有画像和记忆导出是补充来源，不能冒充某个端的基线。

至少两个不同端的有效基线到齐后，才能把结果称为“跨端统一版本”。只有一个端时可以创建本地草稿，但回执必须写“尚未形成跨端基线”。内容为空的有效基线可以计入，因为“当前没有可输出内容”也是该端的真实结果。

### 合并并建立第一版

Codex 校验来源、格式和重复包后，按本文的合并规则生成第一版统一资产，写入消费清单并创建一个 Git commit。第一版成功不以所有端都完成原生记忆吸收为前提。

### 送回并逐端确认

Codex 为每个已纳入端生成投影并尝试送回。初始化回执必须逐端说明：

- 基线是否取得；
- 是否已进入统一版本；
- 投影是否生成；
- 是否已送达；
- 当前上下文是否加载；
- 原生记忆吸收是否可观察。

某个端失败不抹掉其他已完成状态，也不能被压缩成一个笼统的“同步成功”或“同步失败”。

## 日常同步体验

日常同步统一从 Codex 完成协调。用户可以在 Codex 发起，也可以先在某个来源端说“倾倒知界”，再让 Codex 接手导入。

### 来源端先保护自己的新增信息

在接收本轮新投影之前，来源端先生成或暴露自上次成功采集边界以来的新内容。这样可以降低把上一轮投影再次当作新证据倒回来的风险。

“无变化”不能只与上次统一版本比较。实现必须同时考虑：

- 本端上次成功导出或被采集的位置；
- 已经导入和消费的本端包；
- 上次送给本端的投影；
- 本端能否提供稳定的会话、消息或文件游标。

没有稳定游标时，不得把结果声称为精确增量；应生成带范围说明的快照，由 Codex 做来源感知的差集和回声消除。

### Codex 导入、合并和提交

一次协调事务按以下顺序进行：

1. 检查本地实例、互斥锁、Git 工作区和上次未完成状态；
2. 收集用户本轮授权范围内的新包或新会话内容；
3. 校验并导入唯一、不可变的倾倒包；
4. 读取当前统一资产和所有未消费包；
5. 去重、消回声、处理冲突并生成候选变更；
6. 无有效变化时不升版本、不制造空包，只记录 no-op 回执；
7. 有变化时，把统一文件、消费清单、端状态和投影作为一个事务提交；
8. 重新读取实际 commit 和产物，确认发布内容；
9. 按端送回投影并分别记录结果。

本地只有一个规范写入者，因此不再为网页端设计多写者 CAS 发布协议。但同机多个 Codex 任务仍可能竞争，必须通过整轮互斥、干净工作区检查和失败恢复防止互相污染。

### 送回线上端

送回是日常流程的一部分，但不同通道的自动化等级必须如实标记：

- 能用应用内能力完成时，记录目标会话、发送结果和可见消息；
- 需要浏览器时，Codex 可以代用户选择文件、上传和发送，但必须让用户确认账号与目标；
- 页面变化、登录失效、上传失败或无法确认发送时，保留待送达状态并告诉用户下一步；
- 只上传文件但没有让端实际读取，不能记为“已加载”；
- 端在当前会话读过文件，不等于后台原生记忆已吸收。

用户不应长期手工复制大段 Markdown。当前能力确实只能手动完成时，Codex 应把人工动作压缩为登录、授权或最后确认，而不是把搜索、下载、改名、搬运和状态判断全部甩给用户。

## 合并规则

### 先消回声，再谈证据

上一轮送给某个端的投影，下一轮可能被该端原样输出。重复出现不能自动成为第二份独立证据。Codex 必须利用投影记录、来源指针、包标识和采集游标做差集；无法判定时标记不确定，不通过重复次数增加可信度。

### 时间冲突只认条目级依据

倾倒包创建时间、下载时间和导入时间只用于审计，不能证明其中事实更新。可以裁决新旧的依据包括：

- 事实发生时间；
- 用户陈述时间；
- 平台提供的记忆或消息创建时间；
- 明确的 `supersedes`、撤回或更正关系。

没有条目级时间依据的矛盾不得偷偷选一个，进入 `review/` 等待确认。

### 事实冲突与视角互补分开

- 两条信息不能同时为真，且没有足够依据裁决：并列保留证据，正文标记待确认；
- 两条信息可以同时为真，只是来自不同场景：合并成更完整的结论，并保留原始表述和来源。

视角互补是知界的主要价值之一。不同端分别看见用户的一部分，统一资产要把这些部分拼回去，而不是把其中一端覆盖掉。

### 信息分层

每条内容按以下顺序归属：

1. 怎样与用户沟通、什么值得留意 → `routing.md`；
2. 最新事实去哪查、已有资产在哪里 → `sources.md`；
3. 原始材料、来源、冲突和取代关系 → `evidence.md`；
4. 一年后大概率仍成立的提炼结论 → `profile.md`；
5. 会变化或不确定时效的用户状态 → `now.md`；
6. 无法裁决、需要用户确认或失败恢复 → `review/`。

## 状态与回执

以下状态必须分别记录，不能共用一个“已同步”游标：

| 状态 | 回答的问题 |
|---|---|
| 来源已采集 | 本端的新内容是否真的取得 |
| 包已导入 | 内容是否已经进入本地实例且通过校验 |
| 包已消费 | 它是否已影响某个已提交的统一版本 |
| 统一版本已提交 | 权威文件和清单是否已由 Git commit 固定 |
| 投影已生成 | 是否已经为目标端准备材料 |
| 投影已送达 | 文件或消息是否真正到达目标端 |
| 当前上下文已加载 | 目标端是否在当前会话实际读取 |
| 原生记忆已吸收 | 平台是否提供可观察证据；否则只能记“已请求但不可观察” |

这些状态可以同时成立，也可以停在不同位置。例如：“ChatGPT 内容已导入并消费；Claude.ai 投影已生成但上传失败；Codex 当前上下文已加载；两端原生记忆均不可观察。”这是合法结果，不能强选一个互斥终态。

每次运行至少报告：

- 本轮涉及哪些端和数据范围；
- 新增、重复、冲突、跳过和失败分别发生了什么；
- 当前统一 commit；
- 哪些端尚未取得或尚未收到本轮变化；
- 哪些动作需要用户继续；
- 哪些结论只是未验证或不可观察。

## 证据门禁

平台能力结论必须标明日期、账号或产品环境和证据等级：

- `[实测]`：在目标账号与真实工具中完成了所述动作；
- `[官方文档]`：官方明确描述，但尚未在目标账号完成；
- `[用户观察]`：截图或现场操作可见，但复现范围有限；
- `[社区]`：社区报告，只能作为风险线索；
- `[推断]`：由已知事实推导，必须写出推导链；
- `[未验证]`：尚无足够证据。

`未找到`、`404`、工具没有返回内容和模型自述都不能单独证明能力不存在。Agent 必须先查官方资料和公开信息，再做安全的只读探测；只有仍无法确认且确实影响产品决策时，才请求用户做最小必要测试。

README 只能把已经通过对应组合验收的能力写成现状。需求目标、官方存在、账号可见、单步工具可用和完整闭环通过是五件不同的事。

## 安全、隐私与删除

- 未经用户明确授权，不读取跨会话内容，不下载文件，不上传或发送投影，不提交或推送远端；
- 只处理用户有权访问的数据，不要求用户提供密码、会话 Cookie 或其他可复用凭据；
- 默认最小读取范围，扩大到整个 Project、账号历史或目录前再次确认；
- 上传前展示目标端、目标会话或 Project、文件名和内容范围；
- 私人数据默认留在本地，远端备份必须由用户主动开启并验证为私有；
- 不主动猜测哪些信息应该删除。用户要求撤回时停止使用并记录取代关系；用户要求清除原文时，单独确认脱敏范围；
- Git 历史、旧克隆、网页端已吸收的记忆和无法定位的转述不能被虚假承诺为已经彻底删除。

## Alex 首个实例与当前验收组合

Alex 的现有私有实例仓属于上一版架构资产。迁移时应先只读检查，再决定把哪个本地 clone 作为新 `vault/`，或把旧仓作为导入来源与可选远端备份；不得让 ChatGPT 或 Claude.ai 继续直接写统一资产。

当前第一条完整验收路线是：

```text
ChatGPT 指定会话 ─┐
                  ├─> Codex 导入与合并 ─> 本地 Git commit ─> Codex 加载
本地 Agent 倾倒包 ┘                                      └─> 逐端投影与送回
```

随后增加 Claude.ai Skill → 文件下载 → Codex 导入 → 投影上传闭环。它们应共用同一状态机和回执结构，但分别记录通道能力。

验收至少证明：

- 初始化先识别用户已有端、本地目录和旧实例，不再先索要 GitHub 私仓；
- ChatGPT 指定会话能被读取并形成有来源的输入，不依赖 GitHub 插件；
- 本地 Agent 只能写自己的交换目录，不能修改统一资产；
- Codex 能导入至少两个端的基线，保留冲突和互补信息并创建第一版 commit；
- 日常无变化不产包、不升版本，已导入未消费内容不会被再次当成新包；
- 投影送回失败、文件已上传但未加载、当前会话已加载但原生记忆不可观察等状态能同时如实报告；
- 完整闭环不需要网页端取得 GitHub 写权限；
- 任一官方能力变化或实测失败都能被记录为证据更新，而不是被技能悄悄绕过。

## 与外部记忆服务的关系

Hindsight 等服务提供的是独立记忆基础设施，通常通过 API、MCP 或托管服务完成 retain、recall 和反思。知界选择的是另一条路：利用用户已有的 Codex、本地文件和各平台订阅，维护一套可直接审阅的 Markdown/Git 资产，再把结果投影给现有端。

知界的优势不能写成绝对“免费”：用户仍承担已有订阅、模型用量、安装配置和受监督浏览器操作成本。准确的价值主张是：

- 不要求个人用户再购买一个托管记忆服务；
- 数据默认保存在用户本地，可查看、可迁移、可版本化；
- 不要求每个聊天端都安装同一个 MCP 或拥有 GitHub 写权限；
- 适合已经拥有 Codex、愿意用本地控制面换取数据主权和较低新增现金成本的用户。

如果用户需要无 Codex、全天候托管、任意应用 API 接入或完全自动化的记忆基础设施，外部记忆服务比知界更合适。知界不应假装覆盖这类需求。

## 非目标

- 不重建一个通用向量数据库或托管记忆 API；
- 不控制、替换或保证各平台隐藏的原生记忆机制；
- 不恢复从未被任何端保留且无法访问的信息；
- 不把 Alex 的个人目录、画像或账号结构固化为公共模板；
- 不接管 Notion、项目日志、任务系统和日历；
- 不把网页自动化包装成稳定后台接口；
- 不让多个端或多个 Codex 任务同时修改统一资产；
- 不要求日常运行必须 push GitHub；
- 不把纯网页、无 Codex 用户写成当前受支持对象；
- 不在没有证据时声称某个端已接入、某条记忆已写入或完整流程已通过。

## 实现迁移与文档职责

### 当前迁移边界

本需求生效后，上一版实现中的以下规则全部进入待迁移状态：

- GitHub 私仓是必选统一锚点；
- 任意端可以接任协调；
- 每个端都直接写自己的仓库收件区和状态文件；
- 网页端没有仓库写入就属于未接入；
- 多写者 CAS、PR 或自动化发布是正常路径；
- 用户必须从任意端用同一个“同步知界”入口完成全流程。

迁移顺序由 GitHub Project 与 Issues 管理。修复完成前，不得只改 README 宣称新架构已经可用，也不得依据互相冲突的旧文档重画最终用户流程图。

### 仓库里的文档分工

| 路径 | 职责 |
|---|---|
| **本文** | 唯一产品需求：定义目标用户、架构、交互、状态和验收 |
| [GitHub Project](https://github.com/users/Alexu0317-FATHER/projects/3) 与 Issues | 具体问题、依赖、执行状态和验收证据 |
| [`平台能力实测.md`](平台能力实测.md) | 各端能力的日期、环境和证据等级；必须随新架构重新校正 |
| [`../skills/contour/`](../skills/contour/) | 技能实现；当前仍需从旧多写者协议迁移 |
| [`roadmap/`](roadmap/) | 已确认但不进入当前 MVP 的能力 |
| [`history/`](history/) | 已冻结的旧方案、审查快照与交叉评审 |
| [`../README.md`](../README.md) / [`../README.zh.md`](../README.zh.md) | 面向用户的能力、安装、依赖和使用方式；只写已验收内容 |
| [`../CHANGELOG.md`](../CHANGELOG.md) | 每次迭代实际改变了什么 |

本次架构转向的完整决策记录保存在 [Issue #17](https://github.com/Alexu0317-FATHER/contour/issues/17)。后续 Issue 必须引用本次基线 commit 或 tag，不能用默认指向旧分支的模糊文件链接。
