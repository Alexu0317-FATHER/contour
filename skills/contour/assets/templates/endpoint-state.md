# <endpoint-id>

<!-- 放在 state/endpoints/<endpoint-id>.md，只记录这个使用端的加载状态。
     可由该端或获授权代为操作的执行者写入，写明实际执行者；同一使用端并发仍须使用安全写回方式。
     endpoint-id 描述产品表面（chatgpt-web / codex-cli / claude-code……），不含模型名——
     换模型不是换端，模型名放每次倾倒的元数据里。
     倾倒游标不在这里，见该端所用记忆存储的 state/stores/<store-id>.md。 -->

## 基本

- **产品与表面**：
- **使用的记忆存储**：<store-id>
- **当前模型**：
- **账号或项目范围**：
- **原生记忆是否开启**：是 / 否 / 无法确认

## 最近操作记录

<!-- 按实际操作填写，每次同步更新本轮做过的行。一次失败只说明那次操作的结果，
     不据此判定此端以后不能执行；换用等效通道时写明所用通道。
     技能版本取自 SKILL.md 元数据中的 version；“涉及版本”指统一档案的版本。
     “问题反馈”一行只在按 references/feedback.md 起草过反馈时填写，结果写 issue 链接或未提交的状态。 -->

| 操作 | 执行者与所用通道 | 技能版本 | 结果 | 日期 | 涉及版本 | 未完成原因 | 证据位置 |
|---|---|---|---|---|---|---|---|
| 读取已发布档案 |  |  |  |  |  |  |  |
| 读取待处理内容 |  |  |  |  |  |  |  |
| 发布统一结果 |  |  |  |  |  |  |  |
| 问题反馈 |  |  |  |  |  |  |  |

- **本轮同步结果**：双向完成 / 仅完成读取 / 未完成；<原因和待办>

<!-- 本轮写不进档案位置时，本轮双向同步未完成；记录原因，下次可重试或改用等效通道。
     别把「用户每次手工复制粘贴」写成日常通道。 -->

## 指令与记忆加载

- **加载方式**：<实际使用的方式，如文件导入、受管区块、项目文件、只读投影或其他接口>
- **指令更新**：<日期、版本和实际结果>
- **材料送达**：<日期、目标、结果和依据>
- **当前会话读取**：<已读取 / 未读取 / 无法确认；依据>
- **原生记忆状态**：<已观察到跨会话可用 / 未观察到 / 无法确认；依据和范围>
- **最后读到的统一版本**：

## 能力观察（按需）

<!-- 仅在做过 probe.md 的验收或诊断时填写。它们是带日期和条件的可更新证据，不是执行资格；
     未检查项写“未测”。 -->

| 项 | 结果 | 日期 | 条件与原始证据位置 |
|---|---|---|---|
| MV-04 回声隔离 | 通过 / 高风险 / 未测 |  |  |
| MV-05 项目文件隔离 | 通过 / 失败 / 未测 |  |  |
| P-1 负面项形态 | 原样 / 被改写 / 未观察到 / 未测 |  |  |
| P-2 项目文件加载 | 本次生效 / 提示后可读 / 未能读取 / 未测 |  |  |
| P-3 记忆关注策略 | <模型层表现、跨会话表现及对照条件；未测时注明> |  |  |

- **能力分级**：未观察到持久召回 / 前置条件未知 / 可被动召回 / 可自述倾倒 / 可核对导出
- **作用域结论**：
- **污染与回声风险**：
- **下一步**：
