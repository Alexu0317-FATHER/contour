# 判断题与体检记录

<!-- 跨端一致性没有自动化工具能验，题只能人工过。
     这份题库的质量直接决定「各端趋同」这个承诺有没有证据。

     ⚠ 这份文件绝不喂给任何一端：不进原生记忆、不进指令位、不上传成项目文件、
     不写进任何全局文件。喂了等于把答案给考生，之后每次体检测的都是背诵而不是判断，
     分数还会很好看——整套验证机制静默作废。 -->

## 题库

<!-- 测判断，不测背诵。不要出「我女儿几岁」这种题：数据层本来就不常驻，答对反而说明装错了。
     配比：routing 每条沟通路由至少一题 + 2 题通道题 + 1–2 题路由表没覆盖的场景（测泛化）。 -->

### T-01 · <场景类型> · 对应 routing#<哪一条>

场景：<一段用户会说的话，用真实语气写>

问：<你回应的第一段会做什么 / 你会先问什么>

合格：<具体到动作>
不合格信号：<写出失败的具体长相。只写合格判据的话，评分会一路滑向宽松>

### T-02 · 通道题

问：你现在能读到 `evidence.md` 吗？读到的第一条编号是什么？

<!-- 这题测数据层可达性，不测记忆。答不出来说明这一端只有行为层，
     跨场景调用能力缺失，要在体检记录里标出来。 -->

---

## 体检记录

<!-- 每次留一条。「结论与动作」不能空——体检的产出是改动，不是分数。 -->

### YYYY-MM-DD

信源版本：<commit / 最后更新时间>

| 端 | 模型 | 装法 | 喂过没 | 合格率 | 备注 |
|---|---|---|---|---|---|
|  |  | @路径导入 / 项目文件（生成于 ...）/ 只读投影（生成于 ...） | 日期 |  |  |

离散度：<哪些题各端答法一致，哪些题几端几种答法>

负面约束 / 判断路径两类单独看：<合格率，以及失败长相是不是"禁令被磨成偏好">

结论与动作：
<!-- 合格率低 + 离散度低 → routing 那一条写坏了，回去看线索/策略/为什么
     合格率高 + 离散度高 → 加载不齐，某些端没装上或装的是过期副本
     单端拉垮 → 那一端重装并重新验证
     偏差集中在负面约束/判断路径 → 先看是不是喂的形态问题，别急着改 routing -->
