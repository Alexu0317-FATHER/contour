# 实例配置

<!-- 稳定配置。变化很慢，但每次变都要留时间和原因。
     这里不放各端游标——那属于 state/endpoints/<endpoint-id>.md，
     塞进共享文件会制造并发热点。 -->

## 锚点

- **位置**：<私有实例仓的所有者/名称，或链接>
- **默认分支**：
- **私有性确认**：<日期，谁确认的>

<!-- 实例仓必须和技能代码仓分开。实例仓装的是倾倒物（健康、家庭、职业困境），
     技能仓是要公开分发的代码。 -->

## 发布路径

<!-- 三选一，必须实际验证过。版本号只提供观察，不提供互斥——
     必须靠后端的条件写入保证串行。见技能的 protocol.md。 -->

- [ ] 条件直写（提交带 base commit，远端前进则拒绝）
- [ ] 端专属分支 + PR
- [ ] 仓库自动化串行发布

**选定**：
**验证日期**：

## 纳入端

| endpoint-id | 产品与表面 | baseline dump_id | 基线倾倒 | 接入状态 |
|---|---|---|---|---|
|  |  |  | 已完成 / 待完成 | 完整 / 只读 / 未完成 |

<!-- endpoint-id 描述产品表面，不含模型名。换模型不是换端。
     只读 = 读得到统一结果但写不进锚点，没有完成双向同步。
     还有端"待完成基线倾倒"时，产出的画像属于暂定版本，
     不能对用户声称已实现跨端一致。 -->

## 初始化状态

- **已收到有效 baseline 的不同端**：<endpoint-id 列表；同端多包只列一次>
- **状态**：等待第二端基线 / 暂定版本 / 冷启动完成
- **首次统一版本**：尚未发布 / <版本>

<!-- “等待第二端基线”时禁止生成统一文件或消费包。
     至少两个不同的已登记端完成有效 baseline 后，才允许发布第一版暂定版本。
     所有已登记端完成 baseline 后，才改为“冷启动完成”。
     不要求一个网页端加一个本地端；纯网页组合只有在每个端的实际写入与回读路径均已验证，且至少一端具备安全发布路径时才可以。 -->

## 权限边界

<!-- 用户授权了哪些来源可读。默认不读——「能读到」不等于「应该读」。 -->

- **已授权的倾倒源**：
- **明确排除的**：

## 当前统一版本

- **版本**：<门槛未满足时写“尚未发布”，不要伪造 v0>
- **发布于**：
- **发布端**：

## 配置变更记录

<!-- 每条：日期 · 改了什么 · 为什么 -->
