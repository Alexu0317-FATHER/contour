# 实例配置

<!-- 稳定配置。变化很慢，但每次变都要留时间和原因。
     这里不放各端游标——那属于 state/endpoints/<endpoint-id>.md，
     塞进共享文件会制造并发热点。 -->

## 锚点

- **位置**：<私有实例仓的所有者/名称，或链接>
- **默认分支**：
- **私有性确认**：<日期，谁确认的>

<!-- 实例仓必须和技能代码仓分开。实例仓装的是倾倒物（健康、家庭、职业困境），
     技能仓是要公开分发的代码。 -->

## 发布路径

<!-- 三选一，必须实际验证过。版本号只提供观察，不提供互斥——
     必须靠后端的条件写入保证串行。见技能的 protocol.md。 -->

- [ ] 条件直写（提交带 base commit，远端前进则拒绝）
- [ ] 端专属分支 + PR
- [ ] 仓库自动化串行发布

**选定**：
**验证日期**：

## 纳入端

| endpoint-id | 产品与表面 | 基线倾倒 | 接入状态 |
|---|---|---|---|
|  |  | 已完成 / 待完成 | 完整 / 只读 / 未完成 |

<!-- endpoint-id 描述产品表面，不含模型名。换模型不是换端。
     只读 = 读得到统一结果但写不进锚点，没有完成双向同步。
     还有端"待完成基线倾倒"时，产出的画像属于暂定版本，
     不能对用户声称已实现跨端一致。 -->

## 权限边界

<!-- 用户授权了哪些来源可读。默认不读——「能读到」不等于「应该读」。 -->

- **已授权的倾倒源**：
- **明确排除的**：

## 当前统一版本

- **版本**：
- **发布于**：
- **发布端**：

## 配置变更记录

<!-- 每条：日期 · 改了什么 · 为什么 -->
