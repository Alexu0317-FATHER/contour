# 知界配置

<!-- 稳定配置。变化很慢，但每次变都要留时间和原因。
     这里不放游标和加载状态——倾倒游标在 state/stores/<store-id>.md，
     加载状态在 state/endpoints/<endpoint-id>.md，塞进共享文件会制造并发热点。 -->

## 档案位置与接入

- **档案位置类型**：<用户选择的类型，如本地目录、网盘或笔记系统>
- **档案位置**：<用户选择的位置、链接或标识>
- **接入方式**：<实际使用过的读写方式及最近结果；详细记录见各状态文件>
- **可见范围确认**：<日期，谁确认的>

<!-- 档案位置按用户选择填写，不以已有专属实现的名单限制可选位置；接入方式按实际操作记录。
     用户档案必须与随技能分发的代码、模板和示例分开，不把个人运行数据写进技能包。
     未配置接入方式时，记录缺口并引导补齐。 -->

## 发布方式

<!-- 记录实际使用过的发布方式。它须满足 protocol.md 的发布条件：
     统一结果与消费记录同时生效、基础版本变化时拒绝或串行化、发布后可回读。
     版本号只提供观察，不提供互斥。尚未验证写“未验证”。 -->

**方式**：
**满足发布条件的依据**：
**验证日期**：

## 记忆存储

| store-id | 类型 | 位置或账号 | baseline dump_id | baseline 状态 |
|---|---|---|---|---|
|  | 本地 / 云端 |  |  | 待提交 / 已提交 / 已被 <版本> 消费 |

<!-- 记忆存储是 AI 实际保存用户记忆的位置。类型只描述位置（本机或某个账号的云端），
     不决定采集方式；采集方式与实际结果记在 state/stores/<store-id>.md。
     用户自己的画像不登记为记忆存储。store-id 不含模型名。 -->

## 使用端

| endpoint-id | 产品与表面 | 使用的记忆存储 |
|---|---|---|
|  |  |  |

<!-- 使用端是需要读取统一结果的产品入口。endpoint-id 描述产品表面，不含模型名。
     每个使用端默认有自己的记忆存储，不按账号或厂商推定共用；只有用户明确确认两个端是
     同一份记忆时，才填同一个存储。v1 旧包按这里的对应关系换算成 store_id。
     送达、读取和原生记忆状态见 state/endpoints/<endpoint-id>.md。 -->

## 初始化状态

- **已有合规 baseline 的不同记忆存储**：<store-id 列表；同一存储多包只列一次>
- **已被统一版本消费 baseline 的存储**：<store-id 列表>
- **状态**：等待第二个记忆存储 / 暂定版本 / 冷启动完成
- **首次统一版本**：尚未发布 / <版本>

<!-- 三种状态的条件见技能的 references/schema.md。状态只描述基线取得和统一版本保存，
     不表示各使用端已送达或已读取统一结果。
     “等待第二个记忆存储”时只保存配置、状态和未消费包，禁止生成统一文件、草稿画像或消费包。
     门槛不区分本地或云端存储；能否发布取决于本轮是否有满足发布条件的方式，并回读核对。 -->

## 权限边界

<!-- 用户授权了哪些来源可读。默认不读——「能读到」不等于「应该读」；
     已登记也不等于已授权采集。这里写明的日常采集授权，同步时直接复用，不重复询问；
     本轮请求超出这些范围时再确认。授权不代表执行者已经具备访问能力。 -->

- **日常同步可直接采集的存储与范围**：<store-id 及范围>
- **每次需确认的存储或范围**：
- **明确排除的**：

## 当前统一版本

- **版本**：<门槛未满足时写“尚未发布”，不要伪造 v0>
- **发布于**：
- **发布者**：<实际执行发布的端或工具>
- **回读核对**：<日期和结果>

## 配置变更记录

<!-- 每条：日期 · 改了什么 · 为什么 -->
