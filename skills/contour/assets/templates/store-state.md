# <store-id>

<!-- 放在 state/stores/<store-id>.md，只记录这个记忆存储的采集状态。
     任何获授权的端或工具都可以读取该存储并写入本文件，写明实际读取者；
     同一存储并发写回仍须使用安全写回方式。store-id 不含模型名。 -->

## 基本

- **类型**：本地 / 云端
- **位置或账号**：<本地目录，或平台与账号范围>
- **厂商**：
- **对应的使用端**：<endpoint-id 列表>
- **最近的取得方式**：<实际使用的方式，如读取文件、平台对话中自述、官方导出、接口或 MCP；只记录实际做过的>

## 最近采集记录

<!-- 按实际操作填写，每次倾倒更新本轮做过的行。一次失败只说明那次操作的结果；
     换用其他读取者或等效通道时写明。技能版本取自 SKILL.md 元数据中的 version。 -->

| 操作 | 读取者与所用通道 | 技能版本 | 结果 | 日期 | dump_id | 未完成原因 | 证据位置 |
|---|---|---|---|---|---|---|---|
| 取得记忆 |  |  |  |  |  |  |  |
| 提交倾倒包 |  |  |  |  |  |  |  |

## 游标

- **最后一次倾倒**：<dump_id · 日期>
- **未提交的包**：<有就写明保存位置，下次同步重试；不许伪造成功回执>
