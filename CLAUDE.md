# CLAUDE.md

给在这个仓库里干活的 AI 看的。人看 [`README.md`](README.md)（用户）或 [`docs/新知界需求.md`](docs/新知界需求.md)（作者）。

## 这是什么

知界（Contour）是一个跨端记忆同步技能：把各个 AI 端各自记着的用户信息倒出来、合并整理、再喂回每一端，让用户换端时不用重新解释自己。

技能实现在 [`skills/contour/`](skills/contour/)。

---

## 名称约定

**把产品、模式和内置浏览器混为一谈，是这个项目反复出错的地方。** 先把名字钉死：

| 名称 | 指什么 |
|---|---|
| **ChatGPT 桌面应用**（口语里就叫 Codex） | OpenAI 的 Windows / macOS 桌面产品外壳。承载下面全部入口，它是壳，不是执行者 |
| **ChatGPT 模式** | 桌面应用的顶层入口之一。切过去能看到网页版的历史会话与 Projects。Skill 调用符号是 `@` |
| **Codex 模式** | 桌面应用的另一个顶层入口。本地仓库、文件、Git、测试。Skill 调用符号是 `$` |
| **内置浏览器** | 桌面应用内的浏览器面板。**不是 ChatGPT 模式**，也不自动继承桌面应用的登录态 |
| **端 / endpoint** | 任何一个存着用户记忆的地方：网页版 ChatGPT、Claude.ai、Claude Code、Codex、VS Code / Zed 里的 Agent |

用户说"Codex"时通常指整个桌面应用，不是 Codex 模式。**不确定他指哪个就问，别按最窄的意思理解然后据此下结论。**

各名称当下的实际能力，查 [`docs/平台能力实测.md`](docs/平台能力实测.md)。

---

## 三条干活纪律

### 1. 平台能力不许凭印象说

"某个端能不能做某事"——**要么引 [`docs/平台能力实测.md`](docs/平台能力实测.md)，要么去查，不许凭训练数据里的印象填**。这个项目已经因此翻车多次，最近一次是把"网页端没有本地文件的概念"当成边界写进文档，而 `references/load.md` 里明写着配了 MCP / connector 的线上端能读本机文件。

**搜不到 ≠ 做不到。** 平台方几乎从不写"我们不支持 X"，靠搜索拿否定证据这个方法本身就是错的。否定结论只有两种来源算数：官方明确写了不支持，或者我们实际跑了一次被拒绝。

### 2. 结论要标证据等级

`[实测]` / `[官方文档]` / `[用户观察]` / `[社区]` / `[推断]` / `[未查到]` / `[未验证]`，七类不能互相顶替。定义见 [`docs/平台能力实测.md`](docs/平台能力实测.md) 的「记法」节，那里是唯一定义处。

**空着不写就等于说"验过且正常"**，一律写 `[未验证]`。

### 3. 不许伪造成功

跑到哪算哪，卡住就说卡住了。"应该已经上传"不是成功证据。测试失败就贴输出，跳过的步骤要说。

---

## 文档分工

| 路径 | 管什么 | 谁看 |
|---|---|---|
| [`docs/新知界需求.md`](docs/新知界需求.md) | 要什么、为什么、怎么算做完 | 作者 |
| [`docs/平台能力实测.md`](docs/平台能力实测.md) | 各端当下能做什么，带日期和证据等级 | AI + 作者 |
| [`skills/contour/`](skills/contour/) | 技能实现：流程、文件格式、各端装法 | AI |
| [`docs/NOTES.local.md`](docs/NOTES.local.md) | 当前交接状态 | AI + 作者 |
| [`docs/roadmap/`](docs/roadmap/) | 确认要做但不在当前范围 | 作者 |
| [`docs/history/`](docs/history/) | 冻结的旧方案，**只用于追溯，不参与当前决策** | —— |
| [`README.md`](README.md) / [`README.zh.md`](README.zh.md) | 给用户看的：这技能好不好用。**只写已验收的** | 用户 |

**改一处不要在另一处留副本。** 同一个事实出现在两个文件里，早晚会分岔。

---

## 当前状态提醒

**[`skills/contour/`](skills/contour/) 还是上一版的 GitHub 多写者架构，跟 [`docs/新知界需求.md`](docs/新知界需求.md) 已经对不上。**

具体地说，`SKILL.md` 的第一条不变量和「唯一同步状态机」还写着 GitHub 私仓、条件写入、多个端临时接任协调。新架构是：信源在本机，谁在本机跑谁就能整理。

**迁移没做完之前，`skills/contour/` 只能当待审计对象，不能反过来推翻需求文档。**

---

## 提交

- 提交信息用中文，说清改了什么、为什么
- 不要在用户没要求时提交或推送
- 当前工作分支不是 `master` 时直接提交；在 `master` 上先开分支
